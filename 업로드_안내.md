# 멀티페이지 전환 — 업로드 안내

## 무엇이 바뀌었나

메뉴를 누르면 **해당 페이지로 이동**합니다. About(소개)과 Contact(연락처)는 그대로 메인 페이지에 남아 있습니다.

| 메뉴 | 이동 위치 |
|---|---|
| About / Contact | `index.html` 안에서 스크롤 이동 (기존과 동일) |
| Education 외 9개 | 각각 별도 페이지 (`education.html`, `career.html` …) |

디자인·색상·레이아웃은 손대지 않았습니다.

## 업로드할 파일 (11개)

```
index.html        ← 기존 파일 교체 (About + Contact만 남음)
education.html    career.html      research.html
projects.html     books.html       teaching.html
awards.html       coursework.html  data.html
gallery.html
```

`content.json`과 `admin.html`은 **그대로 두세요.** 수정할 필요 없습니다.

GitHub 저장소(`hongedutech/sumin-hong-site`) 루트에 11개 파일을 그대로 올리면 됩니다.
(웹에서 `Add file → Upload files`로 한 번에 드래그해도 됩니다.)

## 알아두실 점

- **콘텐츠 수정 방식은 그대로입니다.** admin.html에서 편집 → `content.json` 다운로드 → 교체 커밋. 모든 페이지가 같은 `content.json`을 읽습니다.
- **빈 섹션은 메뉴에서 자동으로 사라집니다.** 예를 들어 갤러리에 사진이 하나도 없으면 Gallery 메뉴가 안 보입니다. (`gallery.html` 파일은 그대로 두면 됩니다.)
- **언어 선택(한국어/English)이 페이지 간에 유지됩니다.** 한국어로 바꾼 뒤 다른 메뉴로 이동해도 한국어가 그대로 유지됩니다.
- **상단 메뉴 순서를 바로잡았습니다.** 기존에는 Contact가 맨 앞에 오도록 거꾸로 표시되고 있었는데, 이제 About → … → Contact 순으로 나옵니다.
- 현재 보고 있는 페이지의 메뉴에는 보라색 표시가 들어갑니다.
- 각 하위 페이지 맨 아래에는 「← 메인으로」 버튼이 있습니다.

## 되돌리려면

기존 `index.html` 한 개만 다시 올리면 원래의 단일 페이지 사이트로 돌아갑니다. 나머지 10개 파일은 남아 있어도 아무 영향이 없습니다.
